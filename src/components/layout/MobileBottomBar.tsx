import React from 'react';

type Props = {}

const MobileBottomBar = (props: Props) => {
  return (
    <div className="fixed bottom-0 left-0 w-full bg-red-300 py-3 z-50">
      bottombar
    </div>
  );
};

export default MobileBottomBar;
